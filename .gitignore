.idea/
.DS_Store
output/
/dist/

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.db
*.bin

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
/bin/*
*.json
# Fork: driver translations spliced into the frontend bundle at image build
# time. Tracked deliberately -- the image build fails without it.
!/drivers/strm_sync/i18n.json
!renovate.json
/build
/data/
/tmp/
/log/
/lang/
/daemon/
/public/dist/*
!/public/dist/README.md

.VSCodeCounter
*.syso